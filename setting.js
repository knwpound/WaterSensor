export async function handleSetting() {
    // Capacity
    var fullCapacity = document.getElementById('full-capacity').value;
    console.log(fullCapacity);
    // get full capacity from <input type="number" name="Capacity" min="0">
}